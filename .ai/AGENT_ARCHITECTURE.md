# Architecture des Agents IA - OpenSpartan Graph

> Documentation complète du système d'agents, serveurs MCP et hooks configurés.

---

## Vue d'Ensemble

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           CURSOR IDE                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                    AGENT PRINCIPAL (Claude Opus)                      │   │
│  │                                                                       │   │
│  │   ┌─────────────┐   ┌─────────────┐   ┌─────────────────────────┐   │   │
│  │   │  Commandes  │   │ Sous-Agents │   │    Serveurs MCP         │   │   │
│  │   │   Custom    │   │   (Task)    │   │                         │   │   │
│  │   └──────┬──────┘   └──────┬──────┘   └───────────┬─────────────┘   │   │
│  │          │                 │                       │                 │   │
│  └──────────┼─────────────────┼───────────────────────┼─────────────────┘   │
│             │                 │                       │                      │
│             ▼                 ▼                       ▼                      │
│  ┌──────────────────┐  ┌───────────┐  ┌────────────────────────────────┐    │
│  │  .cursor/commands │  │  explore  │  │         MCP Servers            │    │
│  │  ├── /plan        │  │  general  │  │  ┌─────────┐  ┌────────────┐  │    │
│  │  ├── /ingest      │  │  Purpose  │  │  │ DuckDB  │  │  Browser   │  │    │
│  │  ├── /query-halo  │  └───────────┘  │  └────┬────┘  └─────┬──────┘  │    │
│  │  └── ...          │                 │       │             │         │    │
│  └──────────────────┘                  └───────┼─────────────┼─────────┘    │
│                                                │             │               │
├────────────────────────────────────────────────┼─────────────┼───────────────┤
│                                                ▼             ▼               │
│                        ┌─────────────────────────────────────────────────┐   │
│                        │              DONNÉES                             │   │
│                        │  ┌─────────┐  ┌─────────┐  ┌─────────────────┐  │   │
│                        │  │ SQLite  │  │ Parquet │  │   Navigateur    │  │   │
│                        │  │metadata │  │ matchs  │  │   (testing)     │  │   │
│                        │  └─────────┘  └─────────┘  └─────────────────┘  │   │
│                        └─────────────────────────────────────────────────┘   │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 1. Sous-Agents (Task Tool)

L'agent principal peut déléguer des tâches à des sous-agents isolés et autonomes.

### Architecture de Délégation

```
┌────────────────────────────────────────────────────────────────┐
│                     AGENT PRINCIPAL                            │
│                     (Claude Opus)                              │
│                                                                │
│   "Explore le système d'authentification ET le cache"         │
│                            │                                   │
│              ┌─────────────┴─────────────┐                    │
│              │      PARALLÉLISATION       │                    │
│              └─────────────┬─────────────┘                    │
│                            │                                   │
│         ┌──────────────────┼──────────────────┐               │
│         ▼                  ▼                  ▼               │
│  ┌─────────────┐   ┌─────────────┐    ┌─────────────┐         │
│  │ Task #1     │   │ Task #2     │    │ Task #3     │         │
│  │ explore     │   │ explore     │    │ general     │         │
│  │ "auth"      │   │ "cache"     │    │ Purpose     │         │
│  └──────┬──────┘   └──────┬──────┘    └──────┬──────┘         │
│         │                 │                   │                │
│         ▼                 ▼                   ▼                │
│  ┌─────────────────────────────────────────────────┐          │
│  │              RÉSULTATS AGRÉGÉS                   │          │
│  │         (retournés à l'agent principal)          │          │
│  └─────────────────────────────────────────────────┘          │
└────────────────────────────────────────────────────────────────┘
```

### Types de Sous-Agents

| Type | Modèle | Usage | Outils Disponibles |
|------|--------|-------|-------------------|
| `explore` | fast | Exploration codebase, recherche patterns | Grep, Glob, Read, SemanticSearch |
| `generalPurpose` | fast | Tâches multi-étapes, modifications | Tous les outils |

### Caractéristiques d'Isolation

```
┌─────────────────────────────────────────────────────────────┐
│                    SOUS-AGENT ISOLÉ                         │
├─────────────────────────────────────────────────────────────┤
│  ✗ PAS d'accès au contexte de conversation parente         │
│  ✗ PAS d'accès aux messages utilisateur                    │
│  ✓ Propre jeu d'outils (lecture, recherche...)             │
│  ✓ Peut être en mode readonly                              │
│  ✓ Retourne un résumé structuré                            │
│  ✓ Peut être repris via son ID                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. Agents Spécialisés (`.cursor/commands/`)

Les commandes custom fonctionnent comme des **agents spécialisés** avec workflows prédéfinis.

### Architecture Hiérarchique des Agents

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│                          ┌───────────────────┐                              │
│                          │   /orchestrate    │                              │
│                          │                   │                              │
│                          │  CHEF D'ORCHESTRE │                              │
│                          │  Point d'entrée   │                              │
│                          └─────────┬─────────┘                              │
│                                    │                                        │
│                    ┌───────────────┼───────────────┐                        │
│                    │               │               │                        │
│                    ▼               ▼               ▼                        │
│             ┌──────────┐    ┌──────────┐    ┌──────────┐                    │
│             │ FEATURE  │    │   BUG    │    │ QUESTION │                    │
│             │ nouvelle │    │  à fixer │    │exploration                    │
│             └────┬─────┘    └────┬─────┘    └────┬─────┘                    │
│                  │               │               │                          │
│                  ▼               ▼               ▼                          │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                        CYCLE DE VIE D'UNE FEATURE                           │
│                                                                             │
│   ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐             │
│   │  /plan   │───▶│/implement│───▶│  /test   │───▶│ /review  │             │
│   │          │    │          │    │          │    │          │             │
│   │ Planifie │    │ Code     │    │ Valide   │    │ Qualité  │             │
│   └──────────┘    └──────────┘    └──────────┘    └──────────┘             │
│        │                               │                                    │
│        │                               ▼                                    │
│        │                         ┌──────────┐                               │
│        │                         │  /debug  │                               │
│        │                         │          │                               │
│        │                         │ Si bug   │                               │
│        │                         └──────────┘                               │
│        │                                                                    │
│        ▼                                                                    │
│   ┌──────────────────────────────────────────────────────────────────────┐ │
│   │                      AGENTS DE SUPPORT                                │ │
│   │                                                                       │ │
│   │  /explore-feature  /ingest  /query-halo  /verify-db  /update-context │ │
│   │                                                                       │ │
│   └──────────────────────────────────────────────────────────────────────┘ │
│                                      │                                      │
│                                      ▼                                      │
│                              ┌──────────────┐                               │
│                              │   /handoff   │                               │
│                              │              │                               │
│                              │  Passation   │                               │
│                              └──────────────┘                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Vue d'Ensemble des Agents

```
.cursor/commands/
    │
    ├── ORCHESTRATEUR (Point d'entrée)
    │   └── /orchestrate    → 🎯 Analyse et délègue aux agents appropriés
    │
    ├── AGENTS PRINCIPAUX (Cycle de développement)
    │   ├── /plan           → 📋 Planification structurée
    │   ├── /implement      → 🔨 Implémentation avec checklist
    │   ├── /test           → 🧪 Génération et exécution tests
    │   ├── /review         → 🔍 Revue de code qualité
    │   └── /debug          → 🐛 Investigation systématique
    │
    ├── AGENTS DE SUPPORT (Opérations)
    │   ├── /explore-feature → 🗺️ Documentation feature
    │   ├── /ingest         → 📥 Ingestion données Halo
    │   ├── /query-halo     → 🔎 Requêtes SQL DuckDB
    │   ├── /verify-db      → ✅ Vérification intégrité
    │   └── /update-context → 📝 MAJ fichiers .ai/
    │
    └── AGENT DE TRANSITION
        └── /handoff        → 🔄 Passation entre sessions
```

### Agent Orchestrateur

#### `/orchestrate` - Chef d'Orchestre 🎯

L'orchestrateur est le **point d'entrée unique** pour toute demande complexe.
Il analyse la demande en langage naturel et délègue aux agents appropriés.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  /orchestrate "Ajoute un graphique des médailles par session"               │
│                                    │                                         │
│                                    ▼                                         │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                         ANALYSE DE LA DEMANDE                          │ │
│  │                                                                         │ │
│  │  1. Lire contexte (.ai/project_map.md, thought_log.md)                 │ │
│  │  2. Classifier la demande                                              │ │
│  │  3. Déterminer le workflow approprié                                   │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                    │                                         │
│                                    ▼                                         │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                      CLASSIFICATION                                     │ │
│  │                                                                         │ │
│  │   "ajoute", "crée"  ──────────▶  FEATURE  ──▶ /plan → /implement       │ │
│  │   "bug", "erreur"   ──────────▶  BUG      ──▶ /debug → /test           │ │
│  │   "comment", "où"   ──────────▶  QUESTION ──▶ /explore-feature         │ │
│  │   "sync", "import"  ──────────▶  DONNÉES  ──▶ /ingest                  │ │
│  │   "requête", "stats"──────────▶  QUERY    ──▶ /query-halo              │ │
│  │                                                                         │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                    │                                         │
│                                    ▼                                         │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                      EXÉCUTION SÉQUENTIELLE                            │ │
│  │                                                                         │ │
│  │   /plan ──▶ /implement ──▶ /test ──▶ /review ──▶ /handoff              │ │
│  │                              │                                          │ │
│  │                              ▼                                          │ │
│  │                        (échec? /debug)                                  │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                    │                                         │
│                                    ▼                                         │
│                        Résultat + MAJ .ai/                                   │
└─────────────────────────────────────────────────────────────────────────────┘
```

**Parallélisation** : L'orchestrateur peut lancer plusieurs sous-agents `Task` en parallèle
pour des explorations indépendantes :

```
/orchestrate "Explore le cache ET le système de sync"
                          │
           ┌──────────────┴──────────────┐
           ▼                             ▼
    ┌─────────────┐               ┌─────────────┐
    │ Task        │               │ Task        │
    │ explore     │               │ explore     │
    │ "cache"     │               │ "sync"      │
    └──────┬──────┘               └──────┬──────┘
           │                             │
           └──────────────┬──────────────┘
                          ▼
                  Résultats agrégés
```

---

### Détail des Agents Principaux

#### `/plan` - Agent Planification 📋
```
┌─────────────────────────────────────────────────────────────┐
│  /plan "Ajouter support multi-joueurs"                      │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 1. Lire contexte (.ai/project_map.md, thought_log)  │    │
│  │ 2. Analyser fichiers à modifier                     │    │
│  │ 3. Identifier risques et dépendances                │    │
│  │ 4. Générer .ai/plans/[date]-[sujet].md              │    │
│  │ 5. Attendre validation avant /implement             │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                   │
│                          ▼                                   │
│                   Prêt pour /implement                       │
└─────────────────────────────────────────────────────────────┘
```

#### `/implement` - Agent Implémentation 🔨
```
┌─────────────────────────────────────────────────────────────┐
│  /implement "feature X" ou /implement (reprend le plan)     │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 1. PRÉPARATION                                      │    │
│  │    □ Lire .ai/current_plan.md                       │    │
│  │    □ Créer branche git si nécessaire                │    │
│  │    □ Vérifier tests existants passent               │    │
│  │                                                      │    │
│  │ 2. IMPLÉMENTATION (ordre de dépendance)             │    │
│  │    □ Modèles Pydantic                               │    │
│  │    □ Infrastructure (DB, Parquet)                   │    │
│  │    □ Repositories                                   │    │
│  │    □ Services/Logic                                 │    │
│  │    □ UI si applicable                               │    │
│  │                                                      │    │
│  │ 3. VALIDATION par fichier                           │    │
│  │    □ Linting OK (ruff)                              │    │
│  │    □ Imports OK                                     │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                   │
│                          ▼                                   │
│                    Prêt pour /test                           │
└─────────────────────────────────────────────────────────────┘
```

#### `/test` - Agent Tests 🧪
```
┌─────────────────────────────────────────────────────────────┐
│  /test "module X" ou /test --all                            │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 1. ANALYSE                                          │    │
│  │    □ Identifier fonctions publiques                 │    │
│  │    □ Identifier cas limites (edge cases)            │    │
│  │    □ Identifier dépendances à mocker                │    │
│  │                                                      │    │
│  │ 2. GÉNÉRATION (si tests n'existent pas)             │    │
│  │    □ Créer tests/test_[module].py                   │    │
│  │    □ Cas nominal + erreurs + edge cases             │    │
│  │    □ Fixtures et mocks                              │    │
│  │                                                      │    │
│  │ 3. EXÉCUTION                                        │    │
│  │    □ pytest tests/test_xxx.py -v                    │    │
│  │    □ Couverture > 80%                               │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                   │
│              ┌───────────┴───────────┐                      │
│              ▼                       ▼                      │
│         ✓ PASS                  ✗ FAIL                      │
│       /review                   /debug                      │
└─────────────────────────────────────────────────────────────┘
```

#### `/review` - Agent Revue de Code 🔍
```
┌─────────────────────────────────────────────────────────────┐
│  /review "fichier" ou /review --staged                      │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ CRITÈRES DE REVUE                                   │    │
│  │                                                      │    │
│  │ □ QUALITÉ                                           │    │
│  │   Nommage, DRY, fonctions courtes, docstrings       │    │
│  │                                                      │    │
│  │ □ ARCHITECTURE                                      │    │
│  │   Séparation responsabilités, pas d'imports circ.   │    │
│  │                                                      │    │
│  │ □ SÉCURITÉ                                          │    │
│  │   Pas de secrets, validation inputs, SQL safe       │    │
│  │                                                      │    │
│  │ □ PERFORMANCE                                       │    │
│  │   Pas de N+1, Polars vs Pandas, cache               │    │
│  │                                                      │    │
│  │ □ TESTABILITÉ                                       │    │
│  │   Fonctions pures, dépendances mockables            │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ RAPPORT                                             │    │
│  │ 🔴 Critiques  │ 🟡 Améliorations  │ 🟢 Points +     │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

#### `/debug` - Agent Débogage 🐛
```
┌─────────────────────────────────────────────────────────────┐
│  /debug "description du bug" ou /debug --error "msg"        │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ MÉTHODOLOGIE SYSTÉMATIQUE                           │    │
│  │                                                      │    │
│  │ 1. REPRODUCTION                                     │    │
│  │    □ Comportement attendu vs observé                │    │
│  │    □ Étapes pour reproduire                         │    │
│  │    □ Cas minimal                                    │    │
│  │                                                      │    │
│  │ 2. COLLECTE D'INDICES                               │    │
│  │    □ Traceback complet                              │    │
│  │    □ Contexte d'exécution                           │    │
│  │    □ Données d'entrée                               │    │
│  │                                                      │    │
│  │ 3. HYPOTHÈSES (par probabilité)                     │    │
│  │    1. Hypothèse A (plus probable)                   │    │
│  │    2. Hypothèse B                                   │    │
│  │    3. Hypothèse C                                   │    │
│  │                                                      │    │
│  │ 4. INVESTIGATION                                    │    │
│  │    □ Logs, breakpoints, inspection                  │    │
│  │                                                      │    │
│  │ 5. CORRECTION + TEST RÉGRESSION                     │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                   │
│                          ▼                                   │
│              Retour à /test pour valider                     │
└─────────────────────────────────────────────────────────────┘
```

### Détail des Agents de Support

#### `/explore-feature` - Documentation Feature 🗺️
```
┌─────────────────────────────────────────────────────────────┐
│  /explore-feature "cache"                                    │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 1. Recherche Grep/Glob                              │    │
│  │ 2. Lecture fichiers clés                            │    │
│  │ 3. → .ai/features/cache.md                          │    │
│  │ 4. MAJ .ai/project_map.md                           │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

#### `/query-halo` - Requêtes DuckDB 🔎
```
┌─────────────────────────────────────────────────────────────┐
│  /query-halo "Top 10 médailles gagnées"                     │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────┐    │
│  │            MCP DuckDB Server                         │    │
│  │                                                      │    │
│  │  ATTACH 'metadata.db' AS meta;                      │    │
│  │  SELECT medal, COUNT(*) FROM ...                    │    │
│  │  JOIN meta.medal_definitions ...                    │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

#### `/handoff` - Passation Session 🔄
```
┌─────────────────────────────────────────────────────────────┐
│  /handoff                                                    │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Génère dans .ai/thought_log.md :                    │    │
│  │                                                      │    │
│  │ ### 2026-01-31 - Migration Parquet                  │    │
│  │ **Contexte** : Demande initiale                     │    │
│  │ **Raisonnement** : Pourquoi cette approche          │    │
│  │ **Décision** : Ce qui a été fait                    │    │
│  │ **Suivi** : Ce qui reste à faire                    │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                   │
│                          ▼                                   │
│           Session suivante peut reprendre !                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. Serveurs MCP (Model Context Protocol)

### Architecture MCP

```
┌────────────────────────────────────────────────────────────────────────┐
│                          CURSOR IDE                                     │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                        AGENT CLAUDE                              │  │
│   │                                                                  │  │
│   │   CallMcpTool("user-duckdb", "query", {sql: "..."})             │  │
│   │                              │                                   │  │
│   └──────────────────────────────┼───────────────────────────────────┘  │
│                                  │                                      │
│                                  ▼                                      │
│   ┌──────────────────────────────────────────────────────────────────┐  │
│   │                    MCP PROTOCOL LAYER                             │  │
│   │              (JSON-RPC over stdio)                                │  │
│   └──────────────────────────────┬───────────────────────────────────┘  │
│                                  │                                      │
│          ┌───────────────────────┼───────────────────────┐             │
│          ▼                       ▼                       ▼             │
│   ┌─────────────┐        ┌─────────────┐        ┌─────────────┐        │
│   │  DuckDB     │        │  Browser    │        │  Browser    │        │
│   │  Server     │        │  IDE        │        │  Extension  │        │
│   │             │        │             │        │             │        │
│   │ user-duckdb │        │ cursor-ide- │        │ cursor-     │        │
│   │             │        │ browser     │        │ browser-ext │        │
│   └──────┬──────┘        └──────┬──────┘        └──────┬──────┘        │
│          │                      │                      │               │
│          ▼                      ▼                      ▼               │
│   ┌─────────────┐        ┌─────────────┐        ┌─────────────┐        │
│   │   SQLite    │        │  Chromium   │        │  Chrome     │        │
│   │   Parquet   │        │  (headless) │        │  Extension  │        │
│   └─────────────┘        └─────────────┘        └─────────────┘        │
│                                                                         │
└────────────────────────────────────────────────────────────────────────┘
```

### Serveurs Configurés

#### 1. DuckDB (`user-duckdb`)

**Configuration** (`.cursor/mcp.json`) :
```json
{
  "mcpServers": {
    "duckdb": {
      "command": "duckdb-mcp-server.exe",
      "args": ["--db-path", ":memory:"]
    }
  }
}
```

**Outils disponibles** :
| Outil | Description |
|-------|-------------|
| `query` | Exécuter une requête SQL |
| `analyze_schema` | Analyser structure des données |
| `analyze_data` | Statistiques sur les données |
| `create_session` | Créer session DuckDB persistante |
| `suggest_visualizations` | Suggérer des visualisations |

**Usage typique** :
```sql
-- Jointure Parquet + SQLite
ATTACH 'data/warehouse/metadata.db' AS meta (TYPE sqlite);

SELECT p.name_fr as playlist, COUNT(*) as matches
FROM read_parquet('data/warehouse/match_facts/**/*.parquet') f
JOIN meta.playlists p ON f.playlist_id = p.uuid
GROUP BY p.name_fr;
```

#### 2. Browser IDE (`cursor-ide-browser`)

**Usage** : Tests frontend/webapp, interaction avec pages web

**Workflow** :
```
browser_navigate(url)
        │
        ▼
browser_lock()          ← Verrouille l'onglet pour interactions
        │
        ▼
browser_snapshot()      ← Capture état de la page
        │
        ▼
browser_click/fill/...  ← Interactions
        │
        ▼
browser_unlock()        ← Libère l'onglet
```

**Outils clés** :
- `browser_navigate` : Navigation vers URL
- `browser_snapshot` : Capture structure DOM
- `browser_click` / `browser_fill` : Interactions
- `browser_tabs` : Gestion onglets

#### 3. Browser Extension (`cursor-browser-extension`)

Similaire au Browser IDE mais utilise l'extension Chrome installée.

---

## 4. Pre-commit Hooks

### Pipeline de Hooks

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            git commit                                    │
│                                │                                         │
│                                ▼                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    PRE-COMMIT HOOKS                               │   │
│  │                                                                   │   │
│  │   ┌─────────────────┐   ┌─────────────────┐   ┌──────────────┐   │   │
│  │   │   QUALITÉ       │   │   SÉCURITÉ      │   │  VALIDATION  │   │   │
│  │   │                 │   │                 │   │              │   │   │
│  │   │ • ruff          │   │ • detect-       │   │ • validate-  │   │   │
│  │   │ • ruff-format   │   │   secrets       │   │   models     │   │   │
│  │   │ • trailing-ws   │   │ • detect-       │   │ • check-ast  │   │   │
│  │   │ • end-of-file   │   │   private-key   │   │ • check-     │   │   │
│  │   │ • check-yaml    │   │                 │   │   imports    │   │   │
│  │   │ • check-json    │   │                 │   │              │   │   │
│  │   └────────┬────────┘   └────────┬────────┘   └──────┬───────┘   │   │
│  │            │                     │                    │           │   │
│  │            ▼                     ▼                    ▼           │   │
│  │   ┌──────────────────────────────────────────────────────────┐   │   │
│  │   │              ✓ PASS → Commit autorisé                     │   │   │
│  │   │              ✗ FAIL → Commit bloqué + message             │   │   │
│  │   └──────────────────────────────────────────────────────────┘   │   │
│  │                                                                   │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│                                │                                         │
│                                ▼                                         │
│                            git push                                      │
│                                │                                         │
│                                ▼                                         │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                     PRE-PUSH HOOKS                                │   │
│  │                                                                   │   │
│  │   ┌─────────────────────────────────────────────────────────┐    │   │
│  │   │ pytest-fast: pytest tests/ -x -q -m "not slow"          │    │   │
│  │   └─────────────────────────────────────────────────────────┘    │   │
│  │                                                                   │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### Hooks Configurés

#### Pre-Commit (à chaque commit)

| Hook | Description | Auto-fix |
|------|-------------|----------|
| `ruff` | Linting Python (imports, bugs, style) | ✓ `--fix` |
| `ruff-format` | Formatage code Python | ✓ |
| `trailing-whitespace` | Supprime espaces en fin de ligne | ✓ |
| `end-of-file-fixer` | Newline en fin de fichier | ✓ |
| `check-yaml/json/toml` | Validation syntaxe | ✗ |
| `debug-statements` | Détecte `print()`, `pdb`, `breakpoint()` | ✗ |
| `detect-secrets` | Détecte secrets/clés accidentels | ✗ |
| `validate-models` | Valide modèles Pydantic | ✗ |
| `check-imports` | Détecte imports circulaires | ✗ |

#### Pre-Push (avant push)

| Hook | Description |
|------|-------------|
| `pytest-fast` | Tests unitaires rapides (exclu slow/integration) |

### Installation

```bash
pip install pre-commit
pre-commit install                      # Active pre-commit hooks
pre-commit install --hook-type pre-push  # Active pre-push hooks
```

---

## 5. Fichiers de Contexte Agentique (`.ai/`)

### Structure des Fichiers `.ai/`

```
.ai/
 │
 ├── project_map.md      ← Cartographie modules et dépendances
 │
 ├── thought_log.md      ← Journal des décisions (pour passation)
 │
 ├── data_lineage.md     ← Flux de données JSON→SQLite→Parquet
 │
 ├── current_plan.md     ← Plan d'implémentation en cours
 │
 ├── workflows.md        ← Documentation workflows
 │
 ├── MCP_CONFIG.md       ← Configuration serveurs MCP
 │
 └── features/           ← Documentation par fonctionnalité
     ├── README.md
     ├── cache.md
     └── ...
```

### Cycle de Vie Agent

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      DÉBUT DE SESSION                                    │
│                            │                                             │
│                            ▼                                             │
│   ┌────────────────────────────────────────────────────────────────┐    │
│   │  1. Lire CLAUDE.md (instructions générales)                     │    │
│   │  2. Lire .ai/project_map.md (architecture)                      │    │
│   │  3. Lire .ai/thought_log.md (décisions précédentes)             │    │
│   └────────────────────────────────────────────────────────────────┘    │
│                            │                                             │
│                            ▼                                             │
│                       TRAVAIL                                            │
│                            │                                             │
│                            ▼                                             │
│   ┌────────────────────────────────────────────────────────────────┐    │
│   │  4. Mettre à jour .ai/ après modifications significatives       │    │
│   └────────────────────────────────────────────────────────────────┘    │
│                            │                                             │
│                            ▼                                             │
│                      FIN DE SESSION                                      │
│                            │                                             │
│                            ▼                                             │
│   ┌────────────────────────────────────────────────────────────────┐    │
│   │  5. /handoff → Documenter pour prochain agent                   │    │
│   └────────────────────────────────────────────────────────────────┘    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Résumé Rapide

### Infrastructure

| Composant | Fichier/Chemin | Rôle |
|-----------|----------------|------|
| Sous-agents Cursor | Tool `Task` | Délégation tâches (explore, generalPurpose) |
| MCP DuckDB | `.cursor/mcp.json` | Requêtes SQL sur données Halo |
| MCP Browser | Intégré Cursor | Tests frontend |
| Pre-commit | `.pre-commit-config.yaml` | Qualité code automatique |
| Contexte | `.ai/*.md` | Mémoire persistante entre sessions |

### Agents Spécialisés (Commandes Custom)

| Agent | Commande | Rôle | Chaînage |
|-------|----------|------|----------|
| 🎯 **Orchestrateur** | `/orchestrate` | **Point d'entrée - Analyse et délègue** | → Tous |
| 📋 Planification | `/plan` | Planifier avant coder | → `/implement` |
| 🔨 Implémentation | `/implement` | Coder avec checklist | → `/test` |
| 🧪 Tests | `/test` | Générer/exécuter tests | → `/review` ou `/debug` |
| 🔍 Revue | `/review` | Contrôle qualité code | → Merge |
| 🐛 Débogage | `/debug` | Investigation bugs | → `/test` |
| 🗺️ Exploration | `/explore-feature` | Documenter feature | Support |
| 📥 Ingestion | `/ingest` | Charger données Halo | Support |
| 🔎 Query | `/query-halo` | Requêtes DuckDB | Support |
| ✅ Vérification | `/verify-db` | Intégrité données | Support |
| 📝 Contexte | `/update-context` | MAJ fichiers .ai/ | Support |
| 🔄 Passation | `/handoff` | Fin de session | Transition |

### Workflow Typique

```
                    /orchestrate
                         │
         ┌───────────────┼───────────────┐
         ▼               ▼               ▼
      FEATURE          BUG           QUESTION
         │               │               │
         ▼               ▼               ▼
      /plan          /debug      /explore-feature
         │               │
         ▼               │
    /implement           │
         │               │
         ▼               │
      /test ◄────────────┘
         │
         ▼
      /review
         │
         ▼
      /handoff
```

---

*Dernière mise à jour : 2026-01-31*
