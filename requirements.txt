streamlit>=1.28.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0

# Optionnel (CLI / export PNG)
matplotlib>=3.7.0

# Architecture hybride SQLite + DuckDB + Parquet (Phase 2)
# Validation des données API
pydantic>=2.5.0

# Traitement de données haute performance (remplace Pandas pour les gros volumes)
polars>=0.20.0

# Moteur de requête OLAP (jointures SQLite + Parquet)
duckdb>=0.10.0

# Timezone support (requis par DuckDB pour les timestamps)
pytz>=2024.1

# Note: pyarrow est une dépendance de Polars et DuckDB, pas besoin de l'installer séparément

# Tests
pytest>=8.0.0

# RAG (Retrieval-Augmented Generation)
lancedb>=0.6.0
httpx>=0.27.0
